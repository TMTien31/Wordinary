# Your Isle Builder Assets

Runtime PNG and audio assets for the native Wordinary Isle builder.

These assets were imported from the `mykonos-island-voxels` prototype so the
builder can live directly inside `frontend/src/features/isle` without depending
on the standalone prototype folder.

The previous Isle asset pack was removed from this directory.
